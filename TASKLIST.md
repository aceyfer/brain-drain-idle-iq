# BRAIN DRAIN — SHIP TASKLIST (Parent)

> Rule: work top-to-bottom. One task, one commit. Details, decisions, and changelog live in `TASKLIST_DETAILS.md` — go there only when a task needs context.
> Status legend: [ ] open · [~] in progress · [x] done · [!] blocked

## NOW — Shop Collapse Endgame (blocks everything)
- [x] 1. CanvasGuard fix — stop it fighting nested tab canvases + Play-mode guard (details §1)
- [x] 2. Stray giant shop element — get hierarchy path, diagnose, resolve (details §2)
- [x] 3. Paste FindObjectsSortMode console line to Claude Code — confirm harmless (details §3)
- [x] 4. Full clean Play-mode re-test — tabs, buys on all 3 tabs, close/reopen, no stray (details §4)
- [x] 5. LAND THE COMMIT TRAIN — 4, 5, guard, AutoSceneFixes, CanvasGuard, Bible (details §5)
> Note: BP and Cash tabs fully verified working end to end. RP tab superseded by design decision — see §16; not a bug, not blocking.

## NEXT — Stability & Hygiene
- [x] 16. God Shop — COMPLETE end to end. Phase A consolidation (commits `939222f`..`34841b7` + docs `516ce70`); Phase B build-out (`8d15c22` tab UI + owned-toggle, `0f0809c` code-owned tab labels, `9cb8075` rebirth-trigger suppression, `de5d4c0` convert-arrow-glyph cleanup) (details §16)
- [x] 6. BG1-BG6 Sprite Mode Multiple→Single (not Texture Type) — healed 7 dangling refs: 6x stageSprites + SkylineBG m_Sprite (details §6, commit `36c76c1`)
- [x] 7. Doc sync — DONE. Bible §6 rebuilt to 16 buildings (both tabs, defaults quirk, 3 flagged balance anomalies); CLAUDE.md refreshed (16-building reality, costType purchase routing, DioramaManager/COGSWorldPortraitUI marked retired, §20 pacing, 25-tap threshold) (details §7)
- [x] 8. Guard the 12 unguarded Editor tools — CLOSED. All 19 MenuItem entry points guarded via shared `EditorToolGuard` helper, batch commit `e1ba781` (details §8)
- [x] 17. COGS portrait/dialogue visibility — four stacked root causes fixed (details §17, commits `1e01517`/`7fac5d8`/`05c7c13`/`5d3085c`)
- [x] 18. Oversized pedestrians — CLOSED. Three root causes: unnormalized source art + inconsistent hand scales, Animator sprite-swap overriding normalization, rank-figure diorama compositing over the game via Diorama Camera (details §18, commits `aba3091`/`0ec69f9`/`49cda11`/`6d7f709`)
- [x] 20. Dialogue pacing/queue in DialogueManager — CLOSED. Codex findings audited (1 factual error caught); commits: `3e44480` pacing (min-gap + cooldown + coalescing), `780eb8a` shop sort determinism, `06aaa87` legacy walker retirement (details §20)
- [x] 20b. Dialogue log panel + button (GTA-style narrator-line history; also enables anti-repeat) — CLOSED, remaining play-checks moved to §11 (details §20)
- [x] 20c. Chatter bubble fade curve + readability + pedestrian duplicate-NPC fix — CLOSED (details §20)
- [x] 21. World portrait overlap check — CLOSED, verified clear by Aceyfer in Play mode 2026-07-15 (moot anyway: world portrait retired §17/§18); SNOTTING badge sits alone top-right (details §21)
- [x] 27. Pre-existing bug — `CashConverted_VeryResponsible` narrator line described BP→Cash but its trigger only fires on Cash→RP; fixed 2026-08-19 (details §27)
- [x] 28. Pre-existing bug — duplicate convert wiring: `HUDController`'s `convertButton`/`convertUIController` fields confirmed dead via live Play Mode check 2026-08-08, removed (details §28)
- [x] 30. Pre-existing bug — TMP fallback missing U+2794 (right-arrow glyph), substituted a box; 4 occurrences in the Convert panel's scene-authored text, fixed 2026-08-19 (details §30)
- [x] 31. RestorationBarWireFix.cs stale hardcoded defaults (label font, button minHeight, PointsText reparent target) fixed against confirmed live scene values; stale Debug.Log text also corrected (commit `72b1ac7`) (details §31)
- [x] 32. Unity AI Assistant (Muse) relay identified as root cause of historical "mystery" scene drift and disabled in both Brain Drain and Skate ParKour — package removed, Cursor MCP gateway cleared, confirmed dead via relay.txt across multiple restarts (commits `bf80277` Brain Drain, `70bb0c6` Skate ParKour) (details §32)
- [x] 33. Full dialogue pool quality audit (~90 NarratorLine assets across every trigger pool) — confirmed meets the "no stale/basic dialogue" bar; only fix needed was 3 nonsense "snake utters" lines in RandomChatterManager (commit `1a2ac13`) (details §33)
- [x] 34. 6 new stage-themed OfflineDecayReturn welcome-back lines added, one per COGS visual evolution stage (commit `3ee00b2`); stage-tied gating resolved via a manual RestorationPercent band mapping (0-16/17-33/34-50/51-66/67-83/84-100), verified against DialogueManager's real filter (details §34)
- [x] 35. Narrator-line auto-wiring gap — the 6 new §34 stage lines existed on disk but were never wired into DialogueManager.narratorLines (SceneManagerWiring never re-run after §34 shipped); found via full guid audit of all 118 NarratorLine assets vs. the live scene (112/118 wired). Fixed 2026-08-25 by re-running the wiring tool; reverified 118/118 wired, all 6 new lines present (details §35)
- [x] 36. SafeArea re-parenting gap — LogOpenButton and DialogueLogPanel sat as Canvas siblings, not children of CustomSafeArea, so they never got the runtime safe-area inset correction. Fixed 2026-08-26 by reparenting both under CustomSafeArea (commit `dbc69e6`) (details §36)
- [x] 37. Chapter 9 ("The Liberator") PointsSpent gate soft-lock risk — its threshold checked live Points balance, not lifetime spent, contradicting ChapterManager's own stale "nothing spends Points yet" comment. Fixed 2026-08-26 by switching Chapter 9's gate from PointsSpent to WorldRestorationPercent >= 50 (commit `20bc7f4`) (details §37)
- [x] 38. Glass-tube/plunger restoration bar fully removed — RestorationBarPlunger/RestorationBarVesselFrame and their Leonardo-AI art (xp_bar_frame.png, xp_bar_plunger.png) deleted, RestorationBarWireFix.cs no longer builds or wires either object (commit `6cfe4b7`), scene-side GameObjects removed and HUDController.restorationPlungerImage nulled (commit `92e3e10`) (details §38)
- [ ] 39. Editor click-to-select broken — DEPRIORITIZED, not blocking (Aceyfer's call, 2026-08-26): game is fully playable without in-Editor click-to-select; revisit only if it becomes a real bottleneck. Investigated 2026-08-26 — root cause is Editor-local session state, not a code bug (details §39)

## DECISION REQUIRED — Monetization (owner: Aceyfer)
- [x] 10. KILL "neurons" premium currency — DONE. All premium = direct real-world currency via GodTierStoreManager only. Bad Words Pack = $3.99 (details §10)

## THEN — Path to Market
- [x] 23. FTUE / comprehension pass (COGS-narrated onboarding) — SHIPPED & live-tested. FTUEManager + code-built IntelCardUI (`258deee`), FTUE seen-flags in save (`83e8de0`), Beat 9 Illumisnotti name-reveal (`52da27b`/`0b37537`); unblocked §11 (details §23)
- [x] 24. Dialogue log v2 + The Pocket — CLOSED. (a) washout/width/height fixes (`64bc500`/`446af70`/`452eee6`/`26624e1`), (b) COGS/STREET tabs + chatter history + cadence tune (`aa3b298`/`6445834`/`df36d36`), (c) THE POCKET card inventory, derive-from-flags (`36b0d3c`/`23a9707`) — all shipped & (c) Play-mode verified 2026-07-26; (d) post-ship UI fixes — opened-panel z-order + log tab-bar padding so the Dia-Log close-X isn't stolen by the Pocket button and clears the STREET tab (`6142a3d`/`d3ba6f3`) (details §24)
- [x] 11. Define "first playable" cut line — what ships v1, what waits — **PASSED**. First-playable achieved 2026-07-23, Aceyfer verdict (details §11)
- [x] 26. Economy display/design audit — CLOSED. Jumper Cables/IQ Overclock Chip descriptions fixed, PlayerIQManager subscribe guard added (`f0ba24d`); Overcharged IQ system + Underground Economy's 9BP/7Cash split doc-synced into CLAUDE.md/Bible §6; remaining findings confirmed intentional, no action needed (details §26)
- [ ] 12. Real IAP wiring for direct-currency purchases (platform store) — PLANNED 2026-08-31, not started. Full integration plan written (`Assets/Plans/iap-integration-plan.md`): Unity IAP package, one Google Play product per God Shop item (9 total, product-ID map drafted), server-authoritative receipt validation recommended over client-only, narrative scope for the eventual `GodTierStoreManager.cs` changes, and the manual Google Play Console setup Aceyfer owns outside the codebase. Blocked on 8 DECISION REQUIRED items before implementation starts (validation/backend owner, player identity/recovery, final product IDs, pricing incl. a $3.99-vs-$5.00 Bad Words Pack discrepancy the plan caught between this file and TASKLIST_DETAILS.md's decision log, pre-IAP save migration, consumable durability, refund/chargeback policy, offline shop behavior, iOS boundary) (details §12)
- [x] 13. Art debt: remaining placeholder art pass — Player Character art debt is now moot, that whole system was removed 2026-08-27 as abandoned scaffolding, not replaced (details §13)
- [!] 14. Device build + test on real phone — BLOCKED on hardware (no Android device owned; iOS unbuildable on Windows). Android-first decided 2026-08-05. Player Settings prep landed (`c6d6b90`) (details §14)
- [ ] 15. Store presence: name/ratings/screenshots/description (details §15)
- [x] 40. Player-favorite question system (pets/food) — DROPPED 2026-08-31 (Aceyfer's call: "not feeling it anymore"). Never got past the delivery-mechanism decision; no scoping or code work had started (details §40)
- [x] 41. Settings menu — DONE. Mute toggle + 4-song picker built and wired (commit 4dfff12), verified in Play mode by Aceyfer 2026-08-27, no console errors. Gear button lives next to Dia-Log, top-right. Note: "Gutters Filled with Light" uses its G1-1 stem as a stand-in since the pack ships no combined "Full" mix for that song — worth a listen to confirm/swap later (details §41)
- [x] 42. Settings/Pocket button overlap — live WireFix audit found `SettingsButton` at y=-400 overlapping runtime-built `PocketOpenButton` at y=-402, allowing one click to open both panels. Settings scene data and `SettingsMenuWireFix` moved below Pocket to y=-464; live Play mode confirmed each button now opens only its own correctly rendered panel.
- [x] 43. RestorationBarWireFix stale vessel height — the current scene and live layout intentionally use a 130px `RestorationVesselRow`, but the one-shot tool still carried the pre-layout 90px constant and would regress the stack if re-run. Tool aligned to the current 130px geometry; fresh Play-mode Inspector verification confirmed vessel Pos Y 0 / Height 130 and interactive row Pos Y 130 / Height 114.4 with a clean in-Play Console. The destructive menu action was not run.
- [x] 44. HUDMobileOverhaul stale migration — its old 14–37.5% HUD bands predate and would overwrite the current `RestorationVesselRow` / `RestorationInteractiveRow` architecture. Converted to a guarded legacy migration that refuses to run when either modern row exists; Unity recompiled cleanly. The destructive rewrite was not run.
- [x] 45. ShopThreeTabWireFix obsolete builder — it targets the retired `ShopPanel` / `BpUpgradesPanel` hierarchy and would rename, clone, clear, and auto-save over the current `ShopRoot` / `Tab_BP` / `Tab_Cash` / `Tab_RP` shop. Added a current-hierarchy guard; Unity recompiled cleanly. The destructive auto-saving builder was not run.
- [x] 46. MainUIControllerWireFix legacy shop overlay — `ShopOverlayShade` came from the original 64%-height bottom-sheet mock but was never serialized into the scene. The current `ShopRoot` is intentionally full-screen, so creating the shade as the last `CustomSafeArea` sibling would either be invisible behind the shop or block the shop above it. The tool now recognizes the full-screen three-tab hierarchy, leaves the optional runtime field null, and omits/removes the obsolete shade while retaining its still-current controller/pedestrian wiring. A real tool run produced no scene diff; fresh Play mode confirmed the shop opens, remains interactive, and closes normally with a clean in-play Console.
- [x] 47. COGSPortraitWireFix audit — the read-only diagnostic confirmed stages 0–5 map in order to `COGs1.png` through `COGs6.jpg`; an independent GUID check matched every texture meta to its `COGSStage` asset reference. The diagnostic changed no files, and the stage-0 portrait rendered correctly in the fresh Play-mode dialogue card. The idempotent repair path remains current and needs no change.
- [x] 48. Exit-Play teardown error — `ShopUIController.OnDestroy()` called `UnsubscribeFromEvents()`, whose final cleanup re-enabled the rebirth trigger after Unity had begun destroying that GameObject. Normal `CloseShop()` already owns restoring the trigger, so teardown now only unsubscribes and never mutates sibling UI. Reproduced before the fix in an idle Play/exit cycle; verified gone afterward in both idle and shop open/close/exit cycles with a clean Console.
- [x] 49. Nudge-arrow regression — "no gold arrow" over the shop tab bar (2026-08-31). Three interim fixes (includeInactive lookup, activeSelf vs activeInHierarchy, per-frame SetAsLastSibling) were necessary but insufficient; real root cause was `Tab_BP`'s nested Canvas (`overrideSorting=true, sortingOrder=1`) rendering above `UINudgePointer`'s overlay, which had no sortingOrder of its own. Fixed by giving `UINudgePointer` its own `overrideSorting=true, sortingOrder=10` Canvas. Live-tested: arrow renders above the tab bar, clamps/bobs correctly, dismisses on purchase, clean 0-error Console. Full investigation writeup in `Assets/Plans/tutorial-direction-and-cogs-trust.md` ("Regression (2026-08-31)").
- [x] 50. THE POCKET didn't live-update while open — `PocketPanelUI.RebuildList()` was only ever called from `Open()`, so a LITERATES card collected while the panel was already open needed a close/reopen to appear. Fixed by adding `FTUEManager.OnLiteratesCardCollected` (fired the instant each `*Seen` flag flips true) and having `PocketPanelUI` subscribe/unsubscribe to it, rebuilding only while `isVisible`. Live-verified 2026-08-31: opened Pocket showing 2 collected cards, triggered a 3rd (Armadillo Sauce, via first-cash-earned) without ever closing the panel, watched the list update to 3 live.
- [x] 51. Chaos-event popup could interrupt an on-screen FTUE card — `RandomEventManager` and `FTUEManager` had zero mutual coordination in that direction (FTUE already deferred to an active chaos popup, but not the reverse): the unconditional `GameManager.OnSecondTick` chaos-event timer could fire `RandomEventUIController`'s popup over an already-open `IntelCardUI` onboarding card. Added `FTUEManager.IsModalShowing` (wraps the existing `modalShowing` field) and gated `RandomEventManager.HandleSecondTick()` on it — defers and re-checks every tick rather than skipping or rescheduling. Live-verified 2026-08-31 via a new Editor-only hook (`DebugCheats.ForceRandomEventCooldownElapsed`, `BrainDrain > Testing > Force Random Event Cooldown Elapsed`): forced the cooldown to elapse while an FTUE card was on screen, confirmed the popup stayed deferred across multiple ticks, then fired within ~1s of the FTUE queue clearing. Note: the originally-hypothesized specific mechanism (raycast-priority modal stacking silently swallowing clicks) was investigated but not cleanly proven — live click delivery in the test environment showed unrelated flakiness that invalidated that specific claim; this fix is justified by the confirmed structural gap, not that unproven mechanism.
- [x] 52. Bad Words Pack ($3.99 IAP) monetization bug — Tier 3 profanity lines in `RandomChatterManager.GetRandomLine()` were additionally gated behind `rankIndex > 3` on top of the pack's own unlock flag, so a player who bought the pack early (the most likely purchase moment) heard zero audible difference until reaching a late-game World Restoration stage, contradicting the store's "Toggle them on/off anytime once owned" description. Fixed 2026-08-31 by removing the extra rank gate — Tier 3 lines now layer in at every rank once `ProfanityUnlockedPrefsKey` is true, matching the intended design (existing tier lines stay, profanity is an added layer, not a late unlock). Compile verified clean.
- [x] 53. Gary ambient bark system retired, folded into a Pocket card — Aceyfer's call 2026-08-31 ("didn't have much of a role outside of a night of drinking... maybe turn it into a card") after confirming the live `GaryBarkManager`/`GaryBarkLibrary`/`GaryBubbleUI` system technically worked but wasn't a confirmed-valuable ambient feature. All three files plus the `GaryBark/` asset folder and the `GaryBubbleUI` scene subtree were removed outright (scene edited via programmatic YAML block-surgery with exhaustive dangling-reference checks, not freehand), leaving zero "Gary" hits project-wide. Replaced with a new one-shot LITERATES card (`IntelCardCatalog.GaryPod2Id`, "POD #2 QUARTERLY REVIEW — SUBJECT: GARY") gated by a new `FTUEManager.garyCardSeen` flag that fires on `WorldRestorationManager.OnRestorationStageChanged` reaching stage index 2 — chosen to echo Gary's own former stage-2 bark eligibility — and fully wired into `SaveManager` persistence (`ftueGaryCardSeen`). Live-verified 2026-08-31 via a temporary Editor menu hook (removed after use): card fires and displays at the real stage-2 threshold (`pointsRequired` 2,132,885 read directly from the stage asset), confirms and dequeues correctly, appears in THE POCKET both immediately via the live-refresh event and after closing/reopening the panel, and survives a full stop/restart Play cycle (save/load round-trip) — all with a clean 0-error Console throughout.
- [x] 54. Pedestrian wobble-to-levitation arc across World Restoration bands — Aceyfer recalled seeing pedestrians levitate at the top stage previously ("supposed to portray South Park animation comedy") but the live code only ever applied a constant South Park-style wobble regardless of `PedestrianBehaviorStage`; the intended fade-to-hover progression didn't exist. Added `BackgroundPedestrianManager.StageWobbleAmplitudeMultiplier` (1.0/0.65/0.3/0.1/0 across SlackJawed..Aware) so the ground wobble fades out band by band as the world heals, and gave the final band (Engaged, >80% restored — "the god stage") a dedicated hover: a symmetric sine (not the walk-cycle's one-directional bounce) raised `godHoverHeightOffset` above the walk baseline with `godHoverAmplitude`/`godHoverFrequency` and a gentle `godHoverSwayDegrees` sway replacing the footstep tilt. Live-verified 2026-08-31 via temporary Editor menu hooks (removed after use) that jumped `CumulativePointsSpentOnRestoration` to 10/50/90% of the 250,000,000 final threshold and temporary per-sample Debug.Log instrumentation (also removed): SlackJawed showed full-strength wobble (Y up to ~14, rotation up to ~±16°), Walking showed the faded-down wobble (~0.3x), and Engaged showed the symmetric hover riding the full ~16–56 Y range with synced ±6° sway — confirmed via exact logged numbers, not just visual inspection. Clean 0-error Console throughout, 16 pre-existing unrelated warnings unchanged.
- [x] 55. Art pass per Aceyfer's request ("universal button borders...evolution stage of 1-6...the Top could use its own unique set...the sidewalk perfectly at peds feet...any mesh to spice things up") — three of the four pieces shipped and live-verified, mesh spec'd but not generated. New `ArtExpansionTool.cs` (procedural, C#-drawn — no image-gen tool available in this session) generates and wires all of it, idempotently. (a) **Universal button borders**, 6-stage set, `UniversalButtonBorderApplier` added to every Button scene-wide via `FindObjectsInactive.Include`, swaps sprite on `WorldRestorationManager.OnRestorationStageChanged`. Hit a real bug getting there: generated border sprites were completely invisible in Play mode despite every Inspector field (sprite, color, alpha, RectTransform) checking out correct — root-caused to `Image.Type.Sliced`'s render math (`spriteBorder px / spritePixelsPerUnit`): the default import PPU of 100 against a 16px slice border produced a sub-pixel 0.16-unit border at this canvas's scale. Fixed by setting `spritePixelsPerUnit = 2f` for border sprites specifically (not top-bar/sidewalk, which are `Image.Type.Simple` and unaffected). Live-verified: clearly visible borders on SHOP/CONVERT/RESTORE + the 4th button. (b) **Top Bar accent art**, 6-stage gradient strip wired onto `TopBG` via `AccentBarStageView` — its `Image.color` was defaulting to black-ish (needed explicit `Color.white`), fixed and live-verified: warm olive/rust gradient bar with "IQ: 99" text now renders correctly. (c) **Sidewalk at pedestrians' feet** — generated and wired to `PedestrianContainer`'s own baseline anchor, but landed completely invisible (a live red-color test produced zero visible change). Root cause was purely positional, not a render bug: the object's pivot was `(0.5, 1)`, making it hang *downward* from the baseline it shares with `PedestrianContainer`'s bottom edge — directly into the SHOP/CONVERT/RESTORE toolbar's own screen space (confirmed by selecting it in the Scene view and seeing its rect exactly overlap the toolbar, fully occluded by those opaque buttons). Fixed by changing pivot to `(0.5, 0)` so it hangs *upward* from the same baseline instead, into `PedestrianContainer`'s own band — i.e. directly behind/under the feet. Live-verified via Scene view framing (rect now sits cleanly above the toolbar) and a Play-mode zoomed screenshot (visible sidewalk band under both pedestrian sprites). Scene saved. (d) **Mesh/crack-tattoo overlay** — not code-generatable (organic detail needs an actual generative model, not procedural C#); wrote a full Leonardo AI generation spec instead (`Assets/Plans/leonardo-mesh-overlay-spec.md`): target surfaces, 6-stage grime-to-clean opacity table matching every other stage-reactive asset's arc, draft prompts, chroma-key-friendly generation settings, and the exact `ArtImportTool.cs` import steps. Generation and the follow-up wiring script are Aceyfer's next step, not scoped as code work yet. **Flagged to Aceyfer, not fixed (pre-existing, out of scope here):** `WorldRestorationManager` only has 3 configured `WorldRestorationStage` assets — stageIndex 0 (Toxic Wasteland), 2 (Patchwork Recovery Zone), 5 (Utopia Achieved) — so stages 1/3/4 can never actually occur in real play. Every 6-stage art set built in this task (borders, Top Bar, and the pre-existing BG1-6 backdrops) can therefore only ever show 3 of its 6 variants until stages 1/3/4 get configured. Not a bug introduced by this task — `BackgroundStageView`'s BG2/BG4/BG5 already sat behind the same ceiling. **Note on git:** this session has no shell access to Aceyfer's machine (file-bridge only), so none of (a)/(b)/(c)'s file changes were committed to git — they're written to disk and Play-mode-verified, but still need a manual `git add`/`git commit` pass on Aceyfer's end before they're durable per this project's own "one task, one commit" rule.
- [x] 56. Brain Freeze re-spacing to 1/3/7 days — Aceyfer's call 2026-08-31 ("48 hours should be 72 so its 1 3 7 spacing"). Audit caught a real pricing-inversion risk first: bumping `BrainFreeze48.asset` from 48h to 72h without moving its $2.50 price would have made the middle tier cheaper per hour ($0.0347/hr) than the top "Deep Freeze" 168h tier ($0.0357/hr), quietly undercutting the flagship item — full math and options (A $2.99 / B $3.49 / C no change) written up in `Assets/Plans/freeze-spacing-and-rewarded-ads-audit.md` Part 1. Aceyfer picked Option A. `BrainFreeze48.asset` updated: `freezeDurationHours` 48→72, `realMoneyPriceDisplay` $2.50→$2.99, `displayName` "Brain Freeze: 48"→"Brain Freeze: 72", description's "Two days"→"Three days". Confirmed clean spacing after the change: 24h $0.0625/hr > 72h $0.0415/hr > 168h $0.0357/hr — ladder order preserved. Also renamed `itemId` `brain_freeze_48`→`brain_freeze_72` and `productId` `...brainfreeze48`→`...brainfreeze72` per Aceyfer's approval, since no product is registered on Google Play yet (the IAP plan's "once sold, permanent" rule doesn't apply yet) — grepped project-wide first to confirm nothing else referenced the old IDs. Physical filename kept as `BrainFreeze48.asset` (this session has file-bridge-only access, no rename capability) — Claude Code should rename the file to match on its next pass, low priority. Still needs the same manual `git add`/`git commit` pass noted in §55 above.
- [ ] 57. Rewarded-ad idle-window recovery for F2P players — Aceyfer's request 2026-08-31 ("wondering how easy it is to get optional ad's to run like 1 ad = 30 mins no click 100, 2 ads - 1 hour... we could use some ads for players who don't have money to participate"). Design-only so far, NOT implemented — full audit + design in `Assets/Plans/freeze-spacing-and-rewarded-ads-audit.md` Part 2. Key design decision: recovery is retroactive, not a pre-booked timer — hooks `PlayerIQManager.OnOfflineDecayApplied` (already fires the "welcome back" narrator line via DialogueManager) to offer a rewarded-ad prompt the moment the player returns and decay has already been computed, rather than making the player guess how long they'll be gone before leaving. Deliberately does NOT reuse `ExtendOfflineDecayWindow()` (Corporate Cloak's mechanic) — that accumulator is permanent by design for one $9.99 purchase, and free repeatable ad-watches feeding the same permanent stat would eventually devalue it with no ceiling; needs its own time-boxed value instead, closer in shape to Brain Freeze's expiry timestamp. Approved ladder: 1 ad = 30 min recovered, 2 ads = 1 hour, linear, capped at +4 hours (8 ads), landing the effective protected window at 12h total on top of the existing free 8h `OfflineDecayMaxHours` — matches Aceyfer's "8 hours... or 12" range. SDK finding: legacy Unity Ads is deprecated; current path is Unity LevelPlay (`com.unity.services.levelplay`), documented compatible with this project's Unity 6000.x line, mediates AdMob rewarded video, and doubles as a second revenue channel independent of IAP. CAVEAT before installing: TASKLIST.md's PARKED section already notes a prior ad SDK ("DataBite Plankton") was removed in commit `c567a7c` with no reason recorded beyond "removed... as an ad SDK" — worth Claude Code checking that commit's message/diff before a new ads package goes in, in case there was a known reason (policy, conflict, store-review issue) worth not repeating. Not yet scoped as an active implementation task: needs the LevelPlay package installed and a design pass on the actual reward-recovery UI before code starts. Effort roughly comparable to the IAP plan (§12): package install, dashboard/account setup, per-platform ad-unit config, GDPR/consent flow (required regardless of EU player share per Google Play policy), callback wired to the new time-boxed recovery value. Lower-stakes than IAP validation-wise — a modified client granting itself a few extra IQ points isn't a real threat the way a faked $9.99 receipt is, so no server-authoritative validation needed here.

## PARKED (do not touch until NOW+NEXT clear)
- ShopTabView virtualization + real purchase routing (dormant by design)
- Shop-3/Points family (PointsShopPanel/CashShopPanel/ConvertPanel) reconciliation
- UIBlockDebugger legacy-Input rewrite (quarantined)
- §25 concept: COGS counterfeits the resistance (endgame content, post-first-playable) — not scoped (details §25)
- DailyCapThrottleOnset's 6 seeded lines are all full-range (0-100 RestorationPercent), same clinical/in-control COGS register — needs a proper tier pass with late-arc variants where COGS is rattled that the population's daily surplus is shrinking as the world heals, matching the existing degrading-tone-arc convention every other trigger's line pool already has
- Literates Pocket intel card: a Literate confidently blaming the daily cap slowdown on something absurd and unrelated, as ironic contrast to COGS's clinical accuracy — idea only, not written or wired
- `NumberFormatter.Format` has no suffix past "Qi" (~10^21) — values beyond that produce an unbounded digit string with no further suffix rollover. Pre-existing, unrelated to the stage-progression rebalance, not urgent given the 250,000,000 terminal threshold
- [x] UI/Companion Tier 6 displayName fix — CLOSED 2026-08-28. CompanionManager.GetOwnedTier() added; CashShopUIController's maxed-state text now shows Tier 6's real displayName/quote ("Illumisnotty Board Seats") instead of a generic hardcoded line. Verified against CompanionTier6.asset directly. Commit 0e57823, pushed.
- UI/Companion: `HotChickSpawner` spawns up to 6 figures but never reads `CompanionTierData` — no link between a spawned sprite and which tier it represents. Wire during the art pass
- UI/Companion: `EffectText` (Cash Shop companion row) has `m_fontSizeMin: 18` above its `m_fontSize: 14` — harmless today since auto-sizing is off, fix before ever enabling text auto-sizing on it
- Build size: delete `Assets/Nature - Essentials` (122 MB, zero scene references, 3D content in a 2D project) — largest easy win
- Build size: CyberWare music ships 929 MB of source WAVs (already Vorbis on import, only referenced clips ship) — reference only the stems actually used, never all 38
- Build size: `Assets/Resources` is 12 KB — keep it that way, anything placed there force-ships regardless of references
- Audio: `BackgroundMusicManager` has a single `backgroundMusicClip` field; CyberWare ships layered stems (Layer-1/2/3/Full + stingers), already owned. Add instrument layers as World Restoration progresses instead of swapping tracks — the world heals and the music fills in
- VFX: no smoke/fog asset exists yet, needed for the six restoration backdrops — URP particles can do this natively with layered soft-noise sprites, alpha tuned per stage, choking at stage 0, clear by stage 5
- VFX: two rain packs installed (Rain Particles, Rainy VFX) — acid rain early, clearing later, tied to restoration stage. Idea captured 2026-08-27 (Aceyfer): Rain Particles can also drive sidewalk-splash particles layered into the pedestrian background scenery, not just overhead weather — worth a look when this gets built.
- VFX: URP color grading per restoration stage on the existing volume profile — sickly desaturated green early, warm/saturated at Utopia, one volume, six presets
- Retention: `SimpleAndroidNotifications` is installed, unused — hook it to the daily engagement cap reset ("population's cognitive surplus replenishes")
- Retention: easter egg for a later chatter pass — a pedestrian line crediting the player with getting someone's IQ to exactly 117, deliberate Halo nod, distinct from the 113 floor
- Leaderboard (decision required before any work): saves are plain JSON on local disk, trivially editable — any leaderboard would be fabricated immediately, needs server-side validation. Options: Game Center + Google Play Games (free, native, no backend, two integrations) vs. Unity Gaming Services Leaderboards (one API for both, free tier adequate). The daily engagement cap's per-day production ceiling gives server-side validation a concrete rule for rejecting impossible scores
- 1 unspecified warning appeared in the Editor console during the 2026-08-04 God Shop price-glyph check (Bug 3, closed — turned out to be a screenshot artifact, not a real defect). Content wasn't captured at the time; if it recurs, get the actual text before investigating
- TMP dynamic-fallback importer warning (investigated 2026-08-29, pending a font-policy decision): the captured `Importer(NativeFormatImporter) generated inconsistent result for asset` targeted `LiberationSans SDF - Fallback.asset`. The asset is intentionally `AtlasPopulationMode.Dynamic`; Play-mode text requested U+2026 ellipsis, U+005F underscore, and U+00D7 multiplication sign, and Unity's bundled TMP 2.0 code persistently queues the resulting glyph/character tables and 512x512 atlas for the project asset. That explains the font's on-disk churn and why text still renders. The warning itself did not recur in the current Editor log or across two forced isolated reimports: both reimports produced the same import/output IDs and left the asset's SHA-256 unchanged. A contemporaneous status-bar `Import Error Code:(4)` was separately traced to a `ShopUIController.cs` source-timestamp race, not this font. No safe source change was made: eliminating the churn requires a deliberate choice to bake the needed glyphs into a static asset, replace the characters, or accept/clear the dynamic cache; do not change population mode or font coverage without that decision. If the warning recurs, capture the full Console entry and Editor log at occurrence before changing the asset
- [x] Build size/unused import: Free UI build package removed 2026-08-30 after approval. The 2026-08-29 audit found 103 untracked files under `Assets/Free UI build package/` (39 prefabs, 2 scenes, 2 scripts, 1 PNG, 1 PDF, and 58 `.meta` files) plus the root `.meta`, with no GUID references from any asset, scene, package, or project setting outside the import itself
- [x] Untracked placeholder sprites removed 2026-08-30 after approval: neither `Assets/_Game/Events/New Sprite.png` (GUID `e1112c4ceaab8ee438087a0f7f3ad550`) nor `Assets/_Game/Sprites/UI/preview.png` (GUID `b96889a2be953bc4298d75c964fd63a6`) had references anywhere in project assets, scenes, packages, or settings
- VoxelBusters Essential Kit integration (assessed 2026-08-29, BLOCKED pending Aceyfer): the working tree does not contain the Essential Kit Unity SDK. `Assets/Plugins/VoxelBusters/` is only an untracked 15-file Android resource-library fragment (five PNGs plus Gradle/XML/property metadata and four folder `.meta` files); there are no VoxelBusters C# scripts/assemblies, settings asset, package manifest/version, or project-side API calls. `ENABLE_VOXELBUSTERS_ESSENTIAL_KIT` is present but uncommitted for Android, Standalone, and iPhone and currently has no consumer. A clean Editor Play/exit smoke pass compiled and ran with zero Console errors or warnings, confirming the fragment is inert, not integrated. Do not commit the fragment or define yet. Needed before implementation: provide/import the complete licensed Essential Kit SDK with a known version and decide which Essential Kit feature(s) this project is meant to use; feature-specific store credentials, IDs, entitlements, and behavior decisions follow after that selection
- Exit-Play-mode error (captured 2026-08-29 during font verification, not investigated): `GameObjects cannot be made active when they are being destroyed`. Surfaced while exiting Play mode; unrelated to the font assets as far as observed. Documentation only — no fix attempted
- UI-layout verification correction (shop-tab gap, 2026-08-29): commit `698f774` edited the three serialized `Tab_*_ScrollView` offsets, but `ShopUIController.Awake()` immediately overwrote those values through `NormalizeTabGeometry()` / `NormalizeTabPanelRect()` on every Play-mode load, so the commit never fixed the runtime layout despite its message claiming Play-mode confirmation. Future UI-layout verification must inspect the rendered object's live Play-mode `RectTransform` values in addition to reviewing serialized diffs and the visual result
- IAP: no package installed, `realMoneyPriceDisplay` is display-only — nothing can actually be purchased. Needs Apple/Google store setup, Unity IAP, receipt validation
- Push/local notification system (captured 2026-08-06, design settled 2026-08-06, NOT scoped, NOT approved for code): the project currently has NO notification system at all. `Assets/Plans/integration-vfx-music.md:85` shows local notifications were deliberately skipped because DataBite Plankton's remote FCM was assumed available — Plankton was removed in c567a7c as an ad SDK, so that assumption is void and the plan is stale. For an idle game this is a real retention lever. DESIGN NOW SETTLED, the following are decisions not options: (a) THRESHOLD IS PLAYER-SET. The player picks the level at which they are notified, via a slider in settings. Because the player chose it themselves, the system cannot feel like nagging — this is the core insight and should not be traded away. Reference: HP-flash threshold settings in RPGs such as MapleStory. (b) THE SLIDER IS EXPRESSED AS A PERCENTAGE OF BRAIN FUNCTION, NOT AS A RAW IQ NUMBER. Two reasons. Thematically, the "humans only use 10% of their brain" claim is a famous and completely false piece of pop-science, so a game satirising stupidity measuring intelligence in a unit that is itself stupid is on-premise, and COGS would use a debunked metric with total bureaucratic confidence. Architecturally, percentage DECOUPLES the settings UI from the IQ constants, so a later IQ rebalance cannot break the slider or introduce an out-of-range clamp bug. Use a slider, not a numberpad — a slider cannot produce an out-of-range value. (c) SCHEDULE-ON-CLOSE, NEVER POLL. Android local notifications are scheduled, not polled, so nothing runs in the background and nothing watches IQ. On `OnApplicationPause` the game branches ONCE: if IQ is above the player's threshold, compute the crossing time from the existing decay rate and schedule one notification for that moment; if IQ is already at floor, skip the math entirely and schedule a long-absence line at a fixed delay instead. That is one `if` and two possible schedules. It only READS the decay rate and adds no new economy math, so it does not touch the balance structure. Side effect worth keeping: a player who never backgrounds the app never gets notified, so the system is self-silencing. (d) FLOOR OVERRIDE per Aceyfer: a player parked at the floor longer than normal should still get a notification — this is the second branch in (c) and is already covered. (e) COPY IS REVERSE PSYCHOLOGY, NEVER A NAG. COGS is relieved the player stopped and actively discourages their return, which pulls a lapsed player back out of spite. 30 lines (25 standard + 5 long-absence) were written by Grok on 2026-08-06 and are LOCKED — do not rewrite, soften or "improve" them. They live in this session's transcript and must be transferred into the repo before that transcript ages out. (f) TITLE/BODY SPLIT REQUIRED. Audit finding: all 30 lines clear the 110-character cap (range 78-101, median 90) but ALL 30 exceed roughly 65 characters, which is where Android collapses a notification body to one truncated line. Every line is built as two sentences where the SECOND carries the payload, so naive single-field display truncates the punchline and destroys the joke. Fix requires zero copy changes: split each line at its existing period, sentence one becomes the notification title, sentence two becomes the body. This reads better than the original because the title lands like a file header stamped on the player. (g) Settings gear uses COGS cog iconography; the on/off toggle is MANDATORY, not optional polish. OPEN QUESTIONS still owed by Aceyfer before this can be spec'd: (1) what is the NORMAL-PLAY IQ floor? The percentage scale cannot be anchored until this is known. 113 is the Brain Freeze floor, which is a PURCHASED state, not the natural one — anchoring the scale to 113 would give non-Brain-Freeze players a slider whose bottom third is unreachable. (2) Is the percentage honest, or satirically wrong? This is a writing call, not an engineering one, and it changes the copy. Candidate settings labels drafted 2026-08-06: "Notify me when I drop below __% brain function" / "Minimum acceptable brain usage: __%" / COGS voice: "How much of your brain do you require? Be realistic." PLATFORM CONSTRAINT: requires the `com.unity.mobile.notifications` package (NOT currently installed) plus the Android 13+ runtime notification permission flow. Google Play penalises over-notifying apps. IMPLEMENTATION FLAG: the player's threshold value is NEW SAVE STATE and therefore needs a save-migration path and Aceyfer's explicit approval before any work.
- Dialogue chapter archive as a Rebirth reward (captured 2026-08-06, refined 2026-08-08, not scoped, not approved): unlock permanent access to cleared-stage dialogue on Snotting instead of a session-only buffer; two open questions (stageIndex-3 unlock timing, surviving the Rebirth reset) block scoping; new save state, needs migration + Aceyfer's approval (details §29)
- Wiley Karn VII, recurring antagonist-grifter (captured 2026-08-06, not scoped, not approved): a futuristic media-empire heir who sells access to esoteric/ancient "forbidden knowledge", introduced at Stage 3. NAME IS SETTLED as Wiley Karn VII. Earlier candidates referencing a real living public figure were rejected on legal grounds — the game is a paid commercial product shipping on Google Play, and a phonetic near-match plus a matching real-world brand plus a roman numeral together constitute an identifiable portrait rather than a parody, which is a takedown risk that is cheap to avoid now and expensive to fix after a store listing exists. Wiley Karn VII keeps the original cadence, and "wiley" does the characterisation for free. Names for the Stage 4-6 successors are deliberately left open for Aceyfer to make more unique later. THE DYNASTY GAG: the roman numeral increments across stages — Karn VII, then VIII, then IX — with a different face each time, identical patter, always the same relic, never acknowledged. The grift outlives the grifter, which is the most Illumisnotti idea in the game and costs one line of copy per appearance. CONTINUITY HOOK: Karn is the librarian from the Stage 1 "The Library" description. Stage 3 reveals that the librarian the player once snuck past is a media-empire heir who has been selling access to the restricted section the entire time, which locks the shop tiers and the character roster together. THE ARTIFACT: Karn's centerpiece relic is branded the ART-I-FACT(TM). Read as three words it is a confession — art, presented as fact — a fabricated relic publicly labelled as fabricated, which nobody notices. CRITICAL EXECUTION NOTE: this pun must be KARN'S branding, not the game's. He named it himself because he thought it sounded profound. The player discovers the self-own; the game never tells the joke. The trademark symbol is load-bearing — he registered it. Per the naming rule above, the Stage 3 terminal tier is therefore: locked banner FORBIDDEN KNOWLEDGE, unlocked name "The Exhibit", description covering access to the Karn Collection centerpiece behind museum glass. PROPOSED MECHANIC, not approved: The Exhibit harvests cognitive surplus from BELIEVING rather than from thinking — high BP yield sourced from crowds staring at a rock a man told them was alien. Same output, inverted input, and thematically airtight given the Illumisnotti profit from stupidity.
- Stage-evolving shop copy + COGS inflation bark (idea captured 2026-08-05, not scoped, not approved): shop item *descriptions* rewrite per World Restoration stage so the copy drifts as the world heals; item *names* change at only one or two breakpoints rather than every stage, because a rare rename lands and a constant one stops registering. Paired COGS trigger fires on stage advance and frames the rising costs as Illumisnotti retaliation — roughly "good job, they saw what you did and inflated everything again" — which makes the existing `costMultiplier` diegetic instead of invisible math. Fits the established degrading-tone-arc convention and the Snott Market Exchange laundering frame. IMPLEMENTATION FLAG: needs new text fields on the building ScriptableObjects, i.e. edits to the same `.asset` files that carry the Bible §6 locked economy values. Schema addition only, no value changes — but close enough to Bible §5 that it requires Aceyfer's explicit written approval before any asset is touched. Cost estimate before committing: 16 buildings x 6 stages = 96 description strings if every stage evolves. EXPANDED 2026-08-06 — add a "classified until reached" state: shop item names and descriptions for stages the player has not yet reached display as redacted/classified rather than as their real copy, so the shop becomes a curiosity surface instead of a price list. Reaching a stage declassifies that tier and reveals both the evolved name and the evolved description, which makes stage advancement feel like a prestige unlock rather than a number going up. This pairs directly with the COGS inflation bark: COGS reacts to the declassification. Design intent per Aceyfer: nothing in the game should feel repetitive except the tapping itself, which is the loop players opt into; everything wrapped around it should keep revealing. Redacted copy costs almost nothing to author since it is alternate strings on the same fields already required by the base idea. EXPANDED AGAIN 2026-08-06 — CLASSIFICATION LADDER for high shop tiers. The top tiers of both the BP and Cash shops carry real security classifications as their locked-state banner, drawn from the actual clearance ladder (Aceyfer held a clearance at MARSOC, so this is authentic rather than invented): CLASSIFIED for low tiers, SECRET for mid, TOP SECRET for high, and FORBIDDEN KNOWLEDGE for the terminal tier. FORBIDDEN KNOWLEDGE is deliberately the top rung because in the real ladder the level above TOP SECRET is not a higher clearance but COMPARTMENTED access, where clearance alone is insufficient and a person must be read into a specific program — which is the Illumisnotti in a single phrase. Prefer the full "FORBIDDEN KNOWLEDGE" string over a truncated "FORBIDDEN" because it reads as knowledge that exists and is being withheld rather than as a locked door; verify it fits at 1080x2400 portrait first, and if it overflows the tile, fall back to "FORBIDDEN" on the tile while keeping the long form in the description body. NAMING RULE, established via the Stage 1 BP terminal tier: the CLASSIFICATION is the lock state, the ITEM NAME is a deliberately boring real place, and the DESCRIPTION is where the occult content leaks in. The Stage 1 BP terminal tier is named simply "The Library", with a description about sneaking past the librarian into the restricted section for occult knowledge (Harry Potter restricted-section register: mundane setting, transgressive act). The comedy and unease both come from the GAP — a maximum-security banner over an item called a library. This pairs directly with the classified-until-reached mechanic above: pre-unlock the player sees only the banner and imagines something enormous, post-unlock they see "The Library", feel briefly cheated, then read the description and understand. That whiplash is the payoff. Same pattern available for other terminal tiers: The Archive, The Basement, The Filing Room, The Annex — all boring, all wrong to be classified.
